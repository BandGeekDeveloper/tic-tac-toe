import React from 'react';

const GameTypes = ({ playerName }) => {
  return (
    <div className='w-full h-screen'>
      Hello {playerName}! This is the page where you will pick your game you
      will play
    </div>
  );
};

export default GameTypes;
